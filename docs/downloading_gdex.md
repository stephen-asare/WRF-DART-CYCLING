Installed the client: pip install gdex-api-client

Saved your GDEX API bearer token into a plain text file named gdex_token.txt in the same directory as your script

https://gdex.ucar.edu/accounts/profile

Created a control file (e.g., sref_control.json or .ctl) that specifies the dataset, dates, spatial bounding box, and specific variables you need.
